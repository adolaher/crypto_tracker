import React from 'react';
import CoinsIndexContainer from './CoinsIndexContainer.js';

const App = (props) => {
  return (
    <CoinsIndexContainer
      data={props}
    />
  );
}

export default App;
